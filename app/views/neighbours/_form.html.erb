<%= form_with(model: neighbour, local: true) do |form| %>
  <% if neighbour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(neighbour.errors.count, "error") %> prohibited this neighbour from being saved:</h2>

      <ul>
        <% neighbour.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.number_field :phone %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :about %>
    <%= form.text_area :about %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
